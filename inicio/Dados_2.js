
let preço = 20.50;
let quantidade = 2;
let desconto = 0.4;
let subtotal =(preço * quantidade) * 0.4
let total = (preço * quantidade) - subtotal

console.log(`preço ${preço}`);
console.log(`quantidade ${quantidade}`);
console.log(`desconto ${desconto}`);
console.log(`total ${total}`);  

// os sinais de compor uma variável do tipo string(texto) + variável do tipo numero mudaram;
// (`desconto &{desconto}`) = para mostrar (string + variável);